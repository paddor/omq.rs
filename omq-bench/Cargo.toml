[package]
name = "omq-bench"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish = false
build = "build.rs"
default-run = "omq-bench"

[[bin]]
name = "omq-bench"
path = "src/main.rs"

[[bin]]
name = "mom_bench"
path = "src/bin/mom_bench.rs"
required-features = ["mom-bench"]

[features]
default = []
mom-bench = [
  "dep:anyhow",
  "dep:async-nats",
  "dep:bytes",
  "dep:iggy",
  "dep:lapin",
  "dep:prost",
  "dep:rdkafka",
  "dep:redis",
  "dep:tokio-stream",
  "dep:tonic",
  "dep:uuid",
]

[dependencies]
anyhow = { version = "1", optional = true }
async-nats = { version = "0.42", optional = true }
bytes = { version = "1", optional = true }
clap = { version = "4", features = ["derive"] }
futures-util = "0.3"
iggy = { version = "0.10", optional = true }
lapin = { version = "2", optional = true }
libc = "0.2"
omq-tokio = { path = "../omq-tokio", default-features = false }
prost = { version = "0.13", optional = true }
plotters = { version = "0.3", default-features = false, features = ["svg_backend", "line_series"] }
rdkafka = { version = "0.38", default-features = false, features = ["cmake-build", "curl-static", "libz", "tokio"], optional = true }
redis = { version = "0.32", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread", "time"] }
tokio-stream = { version = "0.1", features = ["net"], optional = true }
tonic = { version = "0.12", features = ["transport"], optional = true }
uuid = { version = "1", features = ["v4"], optional = true }

[build-dependencies]
protoc-bin-vendored = "3"
tonic-build = "0.12"

[lints]
workspace = true
