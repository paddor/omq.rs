# cppzmq Examples

Small C++ examples using upstream `cppzmq` classes and exceptions against
`omq-libzmq`.

Build and run:

```sh
omq-libzmq/examples/cppzmq/run_all.sh
```

Set `CPPZMQ_CFLAGS="-I/path/to/cppzmq"` to use a checked-out cppzmq header
instead of the distro `cppzmq.pc`.
