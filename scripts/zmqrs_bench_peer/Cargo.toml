[workspace]

[package]
name = "zmqrs_bench_peer"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "zmqrs_bench_peer"
path = "src/main.rs"

[dependencies]
bytes = "1"
libc = "0.2"
tokio = { version = "1", features = ["full"] }
zeromq = { version = "0.6", features = ["ipc-transport"] }
